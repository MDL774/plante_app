name: plante_app
description: Application reconnaissance plantes
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.7
  tflite_flutter: ^0.10.4
  image: ^4.1.3

flutter:
  uses-material-design: true
  assets:
    - assets/model.tflite
    - assets/labels.txt